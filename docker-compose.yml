version: '3.7'

services:

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: password123
      POSTGRES_USER: albon

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
